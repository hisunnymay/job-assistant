services:
  backend:
    environment:
      FRONTEND_ORIGIN: https://sunnydemo.me

  gateway:
    ports: !override
      - "80:80"
      - "443:443"
    volumes: !override
      - ./deploy/nginx.hk-production.conf:/etc/nginx/conf.d/default.conf:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
